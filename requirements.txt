# Dependencies are bootstrapped by reproduce.py when absent.  OpenResearch's
# Debian image enforces PEP 668 before this file is processed, so keeping this
# file package-free lets the fixed project command remain portable.
